
paths:

    # ASSET TEMPLATE.
    asset_preview_directory:
        definition: '@asset_work_root/review'  

    asset_preview_movie_path:
        definition: '@asset_preview_directory/v{version}/{Step}_{Asset}[_p{part}][_v{variant}][_s{subVariant}][_{lod}].v{version}.{movie_extension}'

    asset_smoothShaded_previews:
        definition: '@asset_preview_directory/v{version}/{Step}_{Asset}[_p{part}][_v{variant}][_s{subVariant}][_{lod}]_smoothShaded.v{version}.{movie_extension}'

    asset_smoothShadedWire_previews:
        definition: '@asset_preview_directory/v{version}/{Step}_{Asset}[_p{part}][_v{variant}][_s{subVariant}][_{lod}]_smoothShadedWire.v{version}.{movie_extension}'

    asset_preview_image_path:
        definition: '@asset_preview_directory/v{version}/{Step}_{Asset}[_p{part}][_v{variant}][_s{subVariant}][_{lod}].v{version}.{timestamp}.exr'

    # SHOT TEMPLATE.
    shot_preview_directory:
      definition: '@shot_work_root/review'

    shot_preview_movie_path:
        definition: '@shot_preview_directory/v{version}/{Step}_{Shot}.v{version}.{movie_extension}'

    shot_preview_image_path:
        definition: '@shot_preview_directory/v{version}/{Step}_{Shot}.v{version}.{timestamp}.exr'
